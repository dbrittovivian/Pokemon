// import { selectTrainersHomeDomain } from '../selectors';

describe('selectTrainersHomeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
